server.port=8080

# Caminho base onde os arquivos JSON serão lidos e escritos pelos Repositories
app.data.path=src/main/resources/data

# Permite que o frontend React (porta 5173) consuma a API sem bloqueio de CORS
app.cors.allowed-origins=http://localhost:5173
